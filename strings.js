const para = "Hi i am sanvi i stood 2nd in PI competition and got a frisbe"

// Accessing the characters
console.log(para[3]);

// If you are accessing something you either use . or []

// Know the length of the string
console.log(para.length)

// Accessing the last character
console.log(para[para.length - 1]);


// Getting the index of specific character

// indexOf gives you the first index of the character that it finds.
console.log(para.indexOf("P"));
console.log(para[29]);

// Getting the last character's index
console.log(para.lastIndexOf("i"));

// Making strings uppercase - Sanvi's

console.log(para.toUpperCase())

// Making strings lowercase - Sanvi's

console.log(para.toLowerCase())